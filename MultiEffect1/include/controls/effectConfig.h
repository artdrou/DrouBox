#pragma once

struct OverdriveConfig {
    int knobDrive   = 0;
    int knobTone    = 1;
    int knobLevel   = 2;
    int dipType     = 0;
};

struct TunerConfig {
};

struct GainConfig {
    int knobGain = 0;
};